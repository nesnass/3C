import { BrickDirective } from './brick.directive';

describe('BrickDirective', () => {
  it('should create an instance', () => {
    const directive = new BrickDirective();
    expect(directive).toBeTruthy();
  });
});
